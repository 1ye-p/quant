# Phase 0 走查摩擦总清单 + 设计优先级校准

> **走查日期**：2026-09-19（会话起始 2026-09-06）
> **课题**：A — A 股多因子选股（日频因子打分 → TopN 持仓）
> **方式**：CLI 命令 + curl 调 UI 实际调用的 API（localhost:8000/api/v1）+ 读 UI 源码核对屏幕可达性；未写脚本直连 DuckDB（唯一一次尝试被 API 持锁阻断）。
> **分日志**：[数据层+因子层（前半）](phase0-friction-log-data-factor.md) · [策略/回测层（后半）](phase0-friction-log-strategy-backtest.md)
> **对照设计**：`docs/superpowers/specs/2026-09-16-research-loop-optimization-design.md`（v2.0，Approved）

## 统计摘要

| 站点 | 条目数 | 🔴 | 🟠 | 🟡 | ✅ |
|---|---|---|---|---|---|
| Task 1 数据层 | 8 | 4 | 3 | 1 | 0 |
| Task 2 因子层 | 7 | 2 | 3 | 2 | 0 |
| Task 3 策略/回测层 | 12 | 5 | 4 | 2 | 1 |
| **合计** | **27** | **11** | **10** | **5** | **1** |

> 环境备注：走查前后两半各遭遇一次 DuckDB WAL 损坏（catalog.duckdb.wal）导致 API server 无法启动，需手工移除 .wal 文件自救——同一病灶一次会话间隔内复发（1-1 / 3-1）。

---

## 一、摩擦总表（统一编号，合并两份分日志）

| # | 站点 | 摩擦描述 | 级别 | 自救方式 | 对应设计条目 | 建议 |
|---|------|---------|------|---------|--------------|------|
| 1-1 | CLI status 首跑 | DuckDB WAL 重放内部错误致 CLI 与 API server 双双无法启动；WAL 损坏意味着上次写入可能丢数据且无提示 | 🔴 | 手工移走 .wal 备份（研究员不该做文件系统操作） | 无 → **新增** | 平台级 P0：checkpoint 策略 + 启动自愈，先于一切 Phase |
| 1-2 | CLI 与 API 互斥 | API server 运行时 CLI 报 Conflicting lock（DuckDB 单写者），CLI 与 UI 不可同时用 | 🟠 | 停 API → 跑 CLI → 重启 | 无 → **新增** | CLI 检测锁时降级 read_only 连接或给出明确提示；开源前处理 |
| 1-3 | 覆盖查询 | 无"任意日期区间×股票池"覆盖端点；`/datasets` 只有全集 min/max/asset_count，且 6675 资产含 880xxx 板块指数、数字虚高 | 🟠 | 只能写 SQL 直查 silver | §3.1 数据浏览器（高·用户）+ 新增 universe 清洗 | 维持数据浏览器高优；补股票池清洗 |
| 1-4 | `/datasets/quality` | recent_assets/daily_coverage/bottom_assets 全锚定 CURRENT_DATE，数据 stale 262 天 → 恒为 0/空；质量分 100 与 outlier 4827 并存自相矛盾 | 🟠 | 无法自救 | §3.1 质量回溯（低）→ **上调** | 先修锚定 bug（按数据 max_date 而非今天），再谈回溯 |
| 1-5 | 质量报告下钻 | 详细 scorer 表名白名单不含真实表 silver_prices_1d；`silver_daily` 返回全 0 假报告（吞异常）；分股票/分年度财务覆盖不可得 | 🔴 | 无（白名单写错） | §3.1 数据覆盖盲区 / 质量回溯 → **上调** | 修白名单 + 停止吞异常；覆盖下钻并入数据浏览器 |
| 1-6 | PIT 股票池 | `/universe/pit` 返回 0、`/universe/stats` 返回 `{}`——上市/退市日期缺失，PIT 功能空转，幸存者偏差不可评估 | 🔴 | 无 | §3.1 退市/停牌 PIT 端到端验证（高·研究员） | 维持高；走查实锤功能空转，验证范围加"先把 PIT 端点修活" |
| 1-7 | 增量摄取进度 | 只有 pending/running/success/error 四态 + last_data_date，无 N/6675 粒度；失败仅 last_error 字符串 | 🟡 | 靠 last_data_date 变化推断 | §3.1 摄取进度透出（中） | 维持中（与设计预判一致，痛点真实但轻） |
| 1-8 | 除权跳空验证 | 无 corporate_actions 端点、无复权切换；anomalies 无法区分除权 vs 真实波动，且 top 异常全是 880xxx 指数 | 🔴 | 外部行情软件核对 | §3.1 复权方式未文档化（研究员视角）→ **上调/新增条目** | 复权方式不仅要素文档化，还要可查：corporate_actions 表 + 端点 + 图表复权切换 |
| 2-1 | IC 计算入口 | IC 计算端到端可用（~10s 出全量结果），但 `/available` 因子提交即报"请先物化"，web/src 无任何物化入口，只能 CLI | 🟠 | CLI `factors --all` | §3.2 自定义因子补消费侧（高）关联项 | 物化入口补 UI（或 available 列表只显示已物化因子）；IC 可信度问题见 2-1c |
| 2-1c | IC 可信度 | mean_ic 0.164 对 20 日动量异常高：样本含板块指数、无涨跌停/停牌过滤说明 | 🟠 | 无法自救 | 无 → **新增** | 因子计算默认过滤非股票资产；IC 报告披露样本构成 |
| 2-2 | 因子目录一致性 | `/available` 509（Alpha360 命名）、`/definitions` 635、实际物化 ~20（cQuant 命名），三套名单互不对齐，available 里没有 ret_20d | 🟠 | 试错：报错后才知道哪些能用 | §3.2 因子选择器搜索/分类（高）→ **范围修正** | 真问题是"目录≠可用"而非"无搜索"（搜索框已存在）；修复=名单一致性 |
| 2-3 | 策略构建器选不到研究成果 | FactorSelector 唯一数据源 `/factors/available`：IC 验证过的物化因子、自定义 DSL 因子都不在列表——因子研究→策略闭环断裂 | 🔴 | 无 | §3.2 自定义因子补消费侧（高，D3 决议） | 维持高；走查证实打击面比设计更大（不止 custom，物化因子也不可见） |
| 2-4 | DSL preview 失效 | preview 样本锚 CURRENT_DATE-30d，数据 stale → 永远"无样本数据仅语法验证"；两套表达式语法（Qlib `$close/Ref` vs DSL 小写）易踩坑 | 🟠 | 提交才知道对错 | 无 → **新增** | preview 按数据 max_date 取样；语法错误提示区分两套语法 |
| 2-5 | IC 排行/汇总 | `gold_factor_ic_summary` 表不存在：ic-leaderboard 恒空、ic-status 抛错，FactorsPage IC 面板静默空白，跨因子横向对比（课题 A 核心）无可用视图 | 🔴 | 无 | §3.2 IC 钻取（中）→ **上调并拆分** | IC 钻取（UX）维持中；新增"IC 汇总落表+端点修复"为高优 bug |
| 2-6 | IC→分层导航 | quintile/decay 是同级 tab、selectedFactor 贯穿，1 次点击可达但无引导性直达链接 | 🟡 | 手动切 tab | §3.2 IC 钻取（中） | 维持中；实际痛点比设计描述轻 |
| 2-7 | 相关性矩阵 | 可用但需手动勾选因子；FactorCorrelationMatrix 组件闲置未引用；无高相关告警、无剔除/替代建议 | 🟡 | 肉眼看热图 | §3.2 相关性分组（中） | 维持中 |
| 3-1 | 环境（复发） | WAL 损坏同一病灶复发，间隔一次会话——回测结果落库可靠性存疑 | 🔴 | 手工删 .wal | 同 1-1 → **新增·P0** | 升级为平台级 P0 而非一次性事件（见 1-1） |
| 3-2 | StrategyBuilder 选因子 | 509 个 Alpha360 命名因子不含默认因子 ret_20d/vol_20d，也不含实测物化可用的 5 个因子；课题 A 的"3 动量/波动因子"一个都配不出来；列表给的 STD20 等实测未物化，选了也跑不通 | 🔴 | 无（继承 2-3） | §3.2 自定义因子补消费侧（高） | 维持高；此条确认 2-3 对策略配置的实际打击面：选择面双向幻觉 |
| 3-3 | 多因子权重 | UI 配的多因子权重不进回测引擎：BacktestCreateBody 只有单一 sort_factor，引擎硬编码 `{sort_factor: 1.0}`——3 因子权重表是死配置，实际回测=单因子，结果解释与配置不符 | 🔴 | Scoring 打分路线绕行 | 无 → **新增** | 最危险静默错误；现有 MultiFactor 引擎补权重传递，先于 Phase 3 DSL |
| 3-4 | 运行回测流程 | 端到端顺畅：建策略→回测→轮询 ~15s 出结果，异步+轮询设计良好（唯无进度百分比） | ✅ | — | — | 无需动作；进度百分比可随摄取进度项顺带 |
| 3-5 | 成交明细 | 指标报 199 笔交易但 `/fills`、`/round-trips` 全空：向量化引擎声称持久化 fills 但本次 run 无记录——换手率与成本核算无法核对 | 🔴 | 无 | 无 → **新增** | 修 fills 持久化；这是 TCA/trade-analysis 的数据源 |
| 3-6 | TCA / 压力测试 | `/tca`、`/stress-test` 均 500（疑与 fills 缺失联动）；成本只有 `cost_model_config: "default"` 字符串 | 🟠 | 无 | §3.5 成本假设醒目展示（假设 TCA 已有）→ **前置修复** | 先修 500 再谈展示位；成本参数需在报告可见 |
| 3-7 | 过拟合分析 | 自动分析静默失败；手动 `/analyze` 实锤代码 bug（`'AnalysisRunSpec' object has no attribute 'portfolio_returns'`）；`/analysis` 永远 "No analysis found"，OverfittingTab 无米下锅——"策略可信吗"第一支柱塌了 | 🔴 | 无 | §3.5 一键验证套件（高）的前置依赖 → **新增前置修复** | PSR/DSR/CPCV 链路必须先修活，否则 Phase 4 一键套件第一根支柱就是空的 |
| 3-8 | 基准 | benchmark 默认空 → beta/IR/tracking_error/alpha 恒 null，-42.66% 总收益无对照系；UI 只有一句 hint | 🟠 | RunModal 手填 benchmark id | §3.3 L2 DSL schema 有 benchmark 字段，但现网默认缺失 → **新增小修** | 默认基准（如 csi300）+ RunModal 下拉选择而非手填 id |
| 3-9 | 导出 | `export?format=html/pdf` 双 500，UI PDF 按钮指向必挂端点；JSON 前端可用；Fills CSV 因 3-5 空转；Risk/Tearsheet/TCA 零导出——报告拿不走 | 🔴 | 无 | §3.6 统一导出（中）+ PDF tearsheet 修复（低）→ **上调** | 先修 500（bug 级）；统一导出维持 Phase 4，但"能看不能拿"佐证其价值 |
| 3-10 | 13 tab 分诊 | model-compare/feature-importance/model-diagnostics 是 ML 专属 tab 却挂在每个回测详情下，且把回测 run_id 当 modelVersion 传——非 ML 回测必空/报错；12 个常显 tab 里 3 个稀释注意力 | 🟠 | 忽略这三个 tab | 无 → **新增** | 条件渲染（仅 ML 策略显示）；归入打磨批次 |
| 3-11 | Walk-Forward 可达性 | 仅 engine===walk_forward 才显示 tab；普通回测想看 WF = 重提一次完整回测，无"就地补跑 WF"；无同一策略两模式对比视图 | 🟡 | 重新提交 WF 模式 | §3.5 一键验证套件（高）覆盖 | 维持：一键套件（含 WF 触发）即解药 |
| 3-12 | 敏感性分析 | Overview 内 2 click 可达、路径最短的可信度工具；未深测数值 | 🟡 | — | §3.5 DSL 参数→敏感性自动映射（高） | 维持；现网可达性良好 |

---

## 二、设计 §3.1-3.6 优先级逐项校准

> 结论四类：**维持 / 上调 / 下调 / 新增**。依据列引用摩擦编号。

### §3.1 数据层

| 设计条目 | 原优先级 | 校准结论 | 依据 |
|---|---|---|---|
| 数据浏览器 | 高·用户 | **维持高** | 1-3/1-5：覆盖与下钻目前只能写 SQL 直查，设计预判完全命中 |
| 退市/停牌 PIT 端到端验证 | 高·研究员 | **维持高，范围扩大** | 1-6：PIT 端点实测空转（count=0/stats={}），验证前需先修活 universe 元数据 |
| 摄取进度透出 | 中 | **维持中** | 1-7：与设计预判一字不差，痛点真实但仅 🟡 |
| 质量回溯 | 低 | **上调至高（先修 bug）** | 1-4/1-5：质量报告现在就是错的（锚定 CURRENT_DATE、表名白名单错、吞异常假报告），不是"缺历史回溯"而是当下不可信 |
| （无） | — | **新增：WAL 损坏治理（P0）** | 1-1/3-1：一次会话间隔复发两次，平台可用性与落库可靠性根基 |
| （无） | — | **新增：股票池清洗（剔除 880xxx 板块指数）** | 1-3/1-8/2-1c：覆盖、异常、IC 全部被指数污染，数字系统性失真 |
| 复权方式未文档化（研究员视角段落，未进优先级清单） | 隐含低 | **上调为显式条目（中高）** | 1-8：不仅要素文档化，还需 corporate_actions 可查 + 复权切换；动量类 IC 可信度直接受制于此 |

### §3.2 因子研究

| 设计条目 | 原优先级 | 校准结论 | 依据 |
|---|---|---|---|
| 自定义因子补消费侧（D3） | 高（工作量小可提前） | **维持高，打击面上修** | 2-3/3-2：不止 custom 因子不可见，物化因子、默认因子全都不在 FactorSelector；D3 方向正确且更紧迫 |
| 因子选择器搜索/分类 | 高 | **维持高，但问题重定义** | 2-2：搜索框已存在，真问题是三套名单不一致（available 509 / definitions 635 / 物化 ~20 互不对齐）——修复应为"目录=可用"一致性，搜索/分类是其次 |
| IC 钻取 | 中 | **维持中（拆分）** | 2-6：导航实际仅 1 click，UX 痛点比设计描述轻；但 2-5 的汇总表缺失是另一件事，见新增 |
| 相关性分组 | 中 | **维持中** | 2-7：确认纯靠人肉取舍 |
| 衰减监控接线 | 中 | **维持中（未触发）** | 走查未涉及，无反证 |
| 正交化收敛（D8） | 低 | **维持低（未触发）** | 走查未涉及 |
| （无） | — | **新增：IC 汇总落表 + ic-leaderboard/ic-status 修复（高）** | 2-5：`gold_factor_ic_summary` 表不存在，排行榜恒空/端点抛错——课题 A 核心的跨因子横向比较无可用视图 |
| （无） | — | **新增：物化入口 + DSL preview 修复（中）** | 2-1/2-4：物化只能 CLI；preview 因 stale 数据永远空转；双语法易踩坑 |

### §3.3 自定义策略

| 设计条目 | 原优先级 | 校准结论 | 依据 |
|---|---|---|---|
| L2 声明式策略 DSL | 核心新增 | **维持** | 走查因 3-2 断链未能实际配置多因子，DSL 价值反而被佐证（唯一出路） |
| L3 Python 插件 | 同 Phase 3 | **维持（未触发）** | 走查未涉及 |
| （无） | — | **新增：现有 MultiFactor 引擎权重传递修复（高，先于 Phase 3）** | 3-3：UI 权重配置是死配置、实际回测=单因子——设计只规划了 DSL 的 score 段，未覆盖现网内置路径的静默错误；不修则 Phase 3 之前课题 A 只能做单因子 |

### §3.4 外部/自定义指标

| 设计条目 | 原优先级 | 校准结论 | 依据 |
|---|---|---|---|
| 市场级择时指标（表/导入/PIT 四道防线） | 高 | **维持（未触发）** | 走查路径未涉及外部指标，无新证据 |
| regime 状态机 + 必答清单 + spike（D10） | 高 | **维持（未触发）** | 同上 |
| 个股级 identity factor | 中 | **维持（未触发）** | 同上 |

### §3.5 回测验证

| 设计条目 | 原优先级 | 校准结论 | 依据 |
|---|---|---|---|
| 一键验证套件+决策清单 | 高 | **维持高，加前置依赖** | 3-7：过拟合分析全链路断（代码 bug + 静默失败），一键套件的第一根支柱现在是空的——修复必须前置 |
| DSL 参数→敏感性自动映射 | 高 | **维持高** | 3-12：敏感性是现网路径最短（2 click）的可信度工具，投资回报确认 |
| 择时周期统计警告 | 高（随 3.4） | **维持（未触发）** | 走查未涉及 |
| StrategyRanker 前端接入（D4） | 中（D4 已升） | **维持** | 走查未测；3-4 确认回测列表/详情框架顺畅，接入成本低 |
| （无） | — | **新增：fills 持久化修复（高）** | 3-5：199 笔交易查无成交，TCA/trade-analysis 全饿死，成本模型是否生效不可验证 |
| （无） | — | **新增：tca/stress-test 500 修复（中高）** | 3-6：设计假设"TCA 已有缺展示位"，实测端点直接 500 |

### §3.6 回测详情/决策报告

| 设计条目 | 原优先级 | 校准结论 | 依据 |
|---|---|---|---|
| 决策摘要卡 | 高 | **维持高** | 3-10：12 个常显 tab 里 3 个必然空转稀释注意力——"判决书"需求被佐证 |
| Regime 时间线 tab | 高（随 3.4） | **维持（未触发）** | 走查未涉及 |
| 统一导出 | 中 | **上调（先修 500）** | 3-9：现有 html/pdf 导出双 500、"能看不能拿"是现实而非假设；500 修复是 bug 级应尽早，推广到所有表格维持 Phase 4 |
| AI 研报 | 中（性价比高） | **维持** | 走查未测；3-9 的"报告拿不走"反而加强其价值 |
| 报告可复现性锚点 | 研究员（未分级） | **上调至显式高** | 3-3/1-8：配置与实际执行不符（权重丢弃）、复权方式无交代——血缘锚点正是这类"静默偏差"的解药 |
| 归因再暴露（D2） | Phase 4+ 可选 | **维持（未触发）** | 走查未涉及 |
| 逐笔周期统计 | 中 | **维持（依赖 3-5 修复）** | trade-analysis 现无米下锅 |
| PDF tearsheet 修复 | 低 | **维持低** | 3-9 佐证双挂，但"Markdown 研报可替代"判断仍成立 |

**校准结论统计：维持 17 / 上调 5（质量回溯、复权、统一导出、可复现性锚点、IC 钻拆分后的汇总修复）/ 下调 0 / 新增 13**（WAL 治理、股票池清洗、IC 汇总落表、物化入口+preview、MultiFactor 权重、fills 持久化、tca 500、基准默认值、ML tab 清理、CLI/API 互斥、IC 可信度披露、corporate_actions 可查、覆盖区间端点并入数据浏览器）。

> 无任何条目下调：走查未触发的项（regime/外部指标/衰减监控/正交化）标注"未触发不等于不存在"，谨慎保持原级。

---

## 三、新增摩擦项（设计未覆盖）与建议归属 Phase

| 新增项 | 摩擦编号 | 建议 Phase | 理由 |
|---|---|---|---|
| WAL 损坏治理（checkpoint 策略 + 启动自愈） | 1-1, 3-1 | **Phase 1 之前的热修（P0）** | 两次复发证明非偶然；一切落库可靠性的前提；工作量小（1-2 天） |
| 股票池清洗：剔除 880xxx 板块指数 | 1-3, 1-8, 2-1c | **Phase 1**（数据地基层） | 覆盖/异常/IC 全部失真，属"地基"性质；与退市 PIT 验证同批 |
| 质量报告 bug 修复（锚定日期、表名白名单、吞异常） | 1-4, 1-5 | **Phase 1** | 数据可信依据，与数据浏览器同批先修 bug 再建视图 |
| corporate_actions 表 + 端点 + 复权切换 | 1-8 | **Phase 1**（可裁剪至 Phase 2） | 动量类因子 IC 可信度前提；最小版=复权方式披露，完整版可后移 |
| 覆盖区间×股票池查询端点 | 1-3 | **Phase 1**（并入数据浏览器） | 设计已有数据浏览器，此处明确覆盖查询是第一用例 |
| MultiFactor 引擎权重传递修复 | 3-3 | **Phase 2**（与因子闭环同批，先行于 Phase 3） | 课题 A 当前就需多因子；不依赖 DSL；配合 2-3 修复后 UI 配置才不是死配置 |
| IC 汇总落表（gold_factor_ic_summary）+ 端点修复 | 2-5 | **Phase 2** | 跨因子横向比较是课题 A 核心，与因子名单一致性天然同批 |
| 三套因子名单一致性（目录=可用） | 2-2, 2-1 | **Phase 2** | D3"补消费侧"的自然扩展：合并名单与物化状态对齐 |
| DSL preview 修复（按数据 max_date 取样）+ 双语法提示 | 2-4 | **Phase 2** | 小修；自定义因子进策略前的数值验证关卡 |
| 基准默认值 + RunModal 下拉选择 | 3-8 | **Phase 2**（小修，随手） | IR/alpha/beta 恒 null 使对照分析空转 |
| 过拟合分析链路修复（AnalysisRunSpec bug + 自动路径报错透出） | 3-7 | **Phase 3 期间并行**（Phase 4 前置） | bug 级 1-2 天；不依赖 DSL；Phase 4 一键套件的硬前置 |
| fills 持久化修复 + tca/stress 500 | 3-5, 3-6 | **Phase 3 期间并行**（Phase 4 前置） | 同为回测后端 bug，与 DSL 无依赖；TCA 展示位的前提 |
| ML tabs 条件渲染（仅 ML 策略显示） | 3-10 | **Phase 5**（打磨） | 注意力稀释问题，非闭环阻断 |
| CLI/API DuckDB 互斥（read_only 降级或明确提示） | 1-2 | **Phase 5**（开源前） | 有 workaround（停一跑一），开源体验问题 |
| IC 报告披露样本构成（过滤说明） | 2-1c | **Phase 2**（随名单一致性） | IC 可信度配套，防止"偏乐观无交代" |

---

## 四、Phase 1-5 校准建议（供联合评审，不直接改设计）

1. **Phase 1 之前插入 1-2 天"P0 热修批"**：WAL 损坏治理（1-1/3-1）。两次复发说明平台可用性本身不可靠，任何 Phase 的验证工作都建立在"server 起得来、结果落得下"之上。这是全部 27 条中唯一连设计前提都会动摇的项。
2. **Phase 1（数据地基层）扩容，人天 5-7 → 8-10**：纳入股票池 880xxx 清洗、质量报告 bug 修复（1-4/1-5）、corporate_actions 最小版（1-8，完整复权切换可裁剪至后续）。均为走查 🔴/关键 🟠 且同属"地基"性质，与既有退市 PIT 验证、数据浏览器同批做有协同。
3. **Phase 2（因子闭环层）范围扩大且可提前启动**：原 2-3 天的"补消费侧"应包含三套名单一致性（2-2）、IC 汇总落表（2-5）、MultiFactor 权重修复（3-3）、基准默认值（3-8）——预估升至 4-5 天。Phase 2 不依赖 Phase 1 的外部指标表，可并行排期；它是课题 A 的直接解锁点（当前因子选择/因子组合/结果验证三段全部降级）。
4. **新增"Phase 3.5 后端验证修复批"（2-3 天，Phase 3 期间并行）**：过拟合分析断链（3-7）+ fills 持久化（3-5）+ tca/stress 500（3-6）+ 导出 500（3-9 的 bug 部分）。全是 bug 级修复、不依赖 DSL；若留到 Phase 4 才发现，一键验证套件的核心支柱（PSR/DSR）和 TCA 展示都会空转。
5. **Phase 3 维持原计划**：走查未触发 regime/外部指标路径，无反证；D10 spike（哨兵伪 panel 捷径）仍是最大杠杆，维持不变。唯一提醒：Phase 3 完成前课题 A 只能"单因子 TopN + 敏感性 + Scoring 绕行"（3-3/2-5 叠加结果），预期管理需在评审中明示。
6. **Phase 4/5 微调**：统一导出（3-9）确认价值但 500 修复已提前至 3.5 批，Phase 4 只做"推广到所有表格"；Phase 5 增加 ML tab 清理（3-10）与 CLI/API 互斥（1-2）；PDF tearsheet 维持低优（Markdown 研报替代判断成立）。

---

*本文档为 Phase 0 成功标准 #1（摩擦清单产出 + 优先级校准）的交付物；校准建议经联合评审后由人决定是否落入设计文档 v2.1。*

---

## 处置结论：摩擦 1-1 / 3-1（WAL 损坏）已闭环 ✅

**修复**（2026-09-19，commits `d390cf5` + `455527d`）：
1. **shutdown 优雅关闭**：`_lifespan` 退出时经 `close_catalog()`（缓存探测，未创建不新建）关闭 catalog → DuckDB close 时自动 checkpoint 清空 WAL。实测 SIGTERM 后 WAL 200B → 清除。
2. **周期 CHECKPOINT**：Catalog 内建 daemon 线程，`CQUANT_CHECKPOINT_INTERVAL_SEC`（默认 600s，0=禁用），失败 logger.error 不中断；close 时 join(5s) 后关连接。
3. **启动自愈**：连接失败且错误含 WAL 重放特征（窄匹配：writeaheadlog/wal file/replay）→ 隔离 `.wal.corrupt-<ts>` + 醒目告警（"上次未落盘写入可能丢失，备份于 xxx"）→ 重试。实测损坏 WAL（尾部 checksum 破坏）→ 隔离成功 + 数据存活。Catalog 层实现，CLI 与 API 双路径覆盖。
4. **全路径 close**：CLI 12 处改 `with Catalog(...)`；mcp_server 3 工具 finally close；advisor `_owns_catalog` 守卫；CLI status 冒烟时实际触发了一次自愈（隔离了旧残留 WAL）——机制在真实路径工作。

**测试**：`test_wal_governance.py` 8 项（close清WAL/checkpoint收缩+数据存活/自愈隔离/误报阴性/线程生命周期×3）+ T4 回归脚本（close→WAL 清除、损坏→隔离+可用、27 测试无回归）。

**级别变更**：🔴 → ✅（研究员不再手删文件；损坏自动隔离有告警）。

---

## 处置结论：摩擦 2-1/2-2/2-3/2-4/2-5/3-2/3-3/3-8（因子闭环层 Phase 2）已闭环 ✅

**修复**（2026-09-19，commits `aa2e274` + `463d2b0` + `a061b3a` + `aec0329` + `b0ad767`）：
1. **3-3 MultiFactor 权重**：BacktestCreateBody/RunSpec 加 factor_weights，策略配置优先（body 显式非 None 覆盖），引擎真权重替代硬编码 `{sort_factor:1.0}`；key⊆factors 校验 + 全零拒绝；旧策略 bit-for-bit 不变（5 测试含路由级）。
2. **2-2 名单一致性（C3）**：/available = 已物化 ∪ 可注册（cQuant 命名 + status 标签），Alpha360 降 reference；/definitions 同源；ret_20d 回列表；materialized DISTINCT 加 TTL 缓存。
3. **2-3/3-2 自定义因子（D3）**：custom_factor_loader → ExpressionFactor → materialize 装配注册；/available 合并 is_custom；CRUD 清缓存；builtin 同名 409 防御。
4. **2-5 IC 汇总**：gold_factor_ic_summary DDL + IC 计算 upsert + leaderboard/status 真实读表（空表显式提示）；alert_checker 列名同步修复。
5. **2-4 preview**：锚 max(trade_date)-30d；Qlib 语法检测提示。
6. **3-8 benchmark**：下拉（无基准首项 + 5 指数，实测数据可用），UI 预选 csi300，服务端无静默默认。
7. **选择器**：搜索/已选置顶/materialized 徽标。

**级别变更**：2-1 🟠→✅、2-2 🟠→✅、2-3 🔴→✅、2-4 🟠→✅、2-5 🔴→✅、3-2 🔴→✅、3-3 🔴→✅、3-8 🟠→✅。课题 A 三段断裂（选因子/配权重/看汇总）全部修复。

---

## 处置结论：摩擦 1-8（除权跳空验证 / 复权披露）已闭环（最小版）✅

**修复**（2026-09-19，本批）：
1. **corporate_actions 可查**：新增 `GET /api/v1/datasets/corporate-actions?asset_id=SSE:600036`——按 `ex_date` 升序返回该股历史分红/除权（action_type / cash_amount / ratio / record_date / pay_date / source）；表为空或 asset_id 无记录返回空数组不报错。数据通路（Tushare `pro.dividend` 仅取"实施"状态 → `CorporateActionsUpdater` 幂等 upsert）此前已就绪（平台优化 P2-4），本批补齐消费端点。
2. **复权口径披露**：`docs/user-guide/backtest.md` §4.1 新增"复权口径"小节——如实写明 `adjusted_ohlc_sql` 的实际口径（OHL = 原始价 × adj_factor 前复权；close = COALESCE(adj_close, close × adj_factor)；volume/amount 不复权），并明示**分红以前复权因子平滑体现，非现金再投资口径**；显式分红现金流需从 silver_corporate_actions 自建对照。
3. **异常区分除权 vs 真实波动**：anomalies 排查时可用 ex_date 对照（文档已写操作路径）；叠加此前 880xxx 指数清洗（99b12e8），top 异常被指数污染的问题已消除。

**裁剪说明**（按 Phase 1 校准建议 #2）：完整版"图表复权切换"（前/后复权一键切换）后移，不阻塞 1-8 闭环判定——研究员现在能"查到"每只股票的除权事件 + "知道"口径是什么，最小闭环达成。

**级别变更**：1-8 🔴 → ✅（最小版；复权切换 UI 留作增强项，不再单列摩擦）。
